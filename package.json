{
  "name": "localflow-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first task and document workspace",
  "main": "server/server.js",
  "scripts": {
    "start": "node --env-file=.env server/server.js",
    "dev": "node --env-file=.env server/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
